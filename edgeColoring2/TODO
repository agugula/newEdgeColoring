- Suwaczek z możliwością ręcznego wpisania liczby do ustalenia liczby osobników w populacji
//jest. ile max bysmy chcieli?
/// no tego właśnie nie ma, jest liczba określająca ilość wierzchołków, populacja to co innego (część 
/// algorytmu genetycznego), do tego jest potrzebne drugie takie coś, zakres myślę, że do 300 może być
/// chociaż nie wiem ile się tego używa w takich normalnych algo gen

- Przydało by się zmienić sposób wyboru kolorów, np. ustalić jakąś grupę, bo po 
odpaleniu algorytmu często wychodzą kolory na tyle podobne, że się tego rozróżnić nie da - nie wiem
czy algorytm źle działa, czy te kolory są tak podobne, że się nie da rozróżnić (na razie jest zachłanny)
(to akurat nizebyt wysoki priorytet)
//dodana funckja Vars.parseIntToColor z 10 stalymi kolorami + random jeżeli więcej


- Przydałoby się jakiś stoper, który wyświetla czas od kliknięcia przycisku do zakończenia algorytmu
(z dokładnością do milisekund) jakoś na tym górnym panelu na przykład

- Zwiększenie limitu wierzchołków, może być kosztem zmniejszenia ich rozmiarów na grafie
// ustwilem promien Node'a chyba na minimum
/// DONE

- Można dorzucić wstawianie randomowych krawędzi, będzie łatwiej i szybciej można testować, przy 
prezentacji projektu też się przyda
//DONE

- Przydałby się jakiś napis ile ten graf ma krawędzi